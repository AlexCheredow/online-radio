import React from "react";
import classes from "./RadioInfo.module.css";

const RadioInfo = (img) => {
  return (
    <div className={classes.bg}>
      <h3>название станции</h3>
    </div>
  );
};
export default RadioInfo;
